{namespace com.example.atlassian.stash.plugins.jenkins.jenkinspostreceiverepositoryhook}

/**
 * @param? config
 * @param? errors
 */
{template .view}
    {call aui.form.textField}
        {param id: 'url' /}
        {param value: $config ? $config['url'] : null /}
        {param labelContent: 'Jenkins URL:'  /}
        {param descriptionText: 'The URL of your Jenkins instance' /}
        {param errorTexts: $errors ? $errors['url'] : null /}
    {/call}
{/template}
